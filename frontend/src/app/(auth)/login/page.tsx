// This has almost nothing in it BUT it should be RSC in case you need to fetch data
// It should call login component

import LoginForm from "./login";

export default function LoginPage() {
  return <LoginForm />;
}
